pipeline {
  agent any
  stages {
    stage("build") {
      step {
        echo "Building the app"
      }  
    }
    stage("test") {
      step {
        echo "testing the app"
      }  
    }
    stage("deploy") {
      step {
        echo "Deploying the app"
      }  
    }
  }
}
